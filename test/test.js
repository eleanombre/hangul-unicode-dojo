const chai = require('chai');
const jamoLookUp = require('../src/jamoValues');
const expect = chai.expect;

describe('Unicode from Hangul Jamo', () => {

	it('should have some tests defined!', () => {
		expect(true).to.be.true;
	})

});
